/**
 * Host plugin body — the Schedule tab lives entirely in the browser entry
 * (lib/client.js). The empty apply keeps the optional browser feature
 * addressable from the Loader overlay.
 */
function apply() {}
export { apply };
